# Мы обращаемся к ID категории, который создали в первом файле
GM_HUN_right_struggle = {

    # А вот тут уже само решение
    GM_HUN_right_struggle_status = {
        icon = generic_propaganda

        # Условия видимости самой кнопки (можно продублировать флаг)
        visible = {
            has_country_flag = GM_HUN_right_struggle_unlocked
        }

        # Когда кнопка активна (можно нажать)
        available = {
            always = yes
        }

        cost = 0

        # Что происходит при нажатии
        complete_effect = {
            # Эффекты
        }

        # Чтобы решение не пропадало после нажатия (если это нужно)
        ai_will_do = {
            factor = 0
        }
    }
}